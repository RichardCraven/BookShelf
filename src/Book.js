import React from 'react'
// import { ReactComponent } from '*.svg';

function Book (props) {
    console.log('in book instance, props are ', props)
    return (
        <div>book</div>
    )
}

export default Book;